# Project memory

Running notes on how this project is deployed and why things are set up the way they are — decisions, context and open issues that aren't obvious from the code. `MEMORY.md` is the index.

Secrets (passwords, API keys, sheet/folder IDs, IP addresses) are deliberately **not** stored here — this repository is public. They're in `DEPLOYMENT_NOTES.md` on the host PC (gitignored).
